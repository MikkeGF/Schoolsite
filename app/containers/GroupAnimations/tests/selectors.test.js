// import { selectGroupAnimationsDomain } from '../selectors';

describe('selectGroupAnimationsDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
